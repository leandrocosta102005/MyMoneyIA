"use client";

import { useEffect } from 'react';

export default function MockUserSetup() {
  useEffect(() => {
    // Componente desativado - sem dados mocados
    // Usuários reais serão criados através do cadastro
  }, []);

  return null;
}
